#lang racket

;(if (argument ) the sedond one is evaluated)(if not then the last on is evaluated)
;(cond are like if but more then teo can be used
;let(list od links first,

(let ((a 3) (b 4)) (* a b) (+ a b))